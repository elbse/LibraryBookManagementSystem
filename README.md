# 📚 Library Book Management System (Python OOP)

A simple console-based **Library Book Management System** built using **Object-Oriented Programming (OOP)** in Python.  
This program allows users to manage books, borrow and return them, and view their availability.

---

## 🚀 Features

- Add a new book (title, author, publication year)
- Borrow a book (changes availability to *Not Available*)
- Return a book (changes availability to *Available*)
- Display all books with their status
- 3 books are pre-loaded when the program starts
- Error handling using `try` and `except` to prevent crashes

---

## 🧱 OOP Concepts Used

### **Class: `Book`**
Encapsulates book properties and actions:

#### **Attributes**
- `title`
- `author`
- `publication_year`
- `available`

#### **Methods**
- `borrow_book()`
- `return_book()`
- `display_info()`
- `is_available()`

---

## 

#👤Charisse Priego
